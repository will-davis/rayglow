

1. I need a quick guide on how to toggle between 4-panel display and 8 panel display. This is mostly for A/B testing. I'm struggling to push the frame rate to a desirable level and want to experiment to get an intuition for how changing x affects y. Ideally if I had a single 4 panel row the handshake would agree to use 64kb packets automatically. 
2. The change you made - is it the "word" length adjusting from 64 to 128 based on the config.py panel dimensions or is it static? (or something else). I don't mind reflashing between configuration testing, just curious how accessible it is to toggle more to the point, how to do that if needed. You earlier mentioned `I'll make pack() infer width from the frame it's handed. Its per-row geometry (_shift/_addr_row) depends only on height, so the identical, already-verified packer serves both 256 and 512 with no contract change. The fold is a pure pre-pack transform. Let me implement.` which makes me think it is dynamic.
3. Is there a way to get feedback on which piece of hardware is the FPS limiter? I have been assuming that I am not saturating the SPI link, but I also know that if I run a run an intensive shader, the rpi5 is limiting due to rendering the frame, so not saturation, just waiting on the next frame. It would be nice to know which is clamping the current FPS but I'm not sure how to go about that.
4. My wiring setup. Any advice on mitigating impedance for less signal degradation? Just simple things like "add a 10uf cap between Vcc and GND where they enter the board" or "add a 10ohn resistor to reduce reflection to the clk pin" (I'm making those up, but hopefully it gives you the idea of the types of simple tweaks I'm looking to make). 
5.The 5V 60A input is decoupled from the Adafruit hat and 5V rail from the from the USBs powering the rpi5 and rp2350. I am working on getting it plugged in but would like to make use of the MAX4866T on the hat to keep any potential over voltage spikes from propogating back into the pi and rp2350 and it's a little tricky to wire. If I leave the 5v pins disconnected between the rp2350 and the hat, they should be fully decoupled which would prevent the amp-swinging matrix from sending anything dirty back to the rp2350 and rpi5 correct? That way the 5V rail on the matrix side fluctuates volt for volt and the signal relayed from the logic level shifter hit's the panel's own bit shifters and other ICs at the exact same voltage (e.g. 4.8V and 4.8V instead of 5V from the pi and 4.8 from the power supply loop). Is that sound logic?
